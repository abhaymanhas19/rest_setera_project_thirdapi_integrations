import React from 'react';
import { Outlet } from 'react-router-dom';

type Props = {};

const SupportPageLayout = (props: Props) => {
  return (
    <><Outlet /></>
  );
};

export default SupportPageLayout;