import React from 'react';
import { Outlet } from 'react-router-dom';

type Props = {};

const OrderPageLayout = (props: Props) => {
  return (
    <><Outlet /></>
  );
};

export default OrderPageLayout;