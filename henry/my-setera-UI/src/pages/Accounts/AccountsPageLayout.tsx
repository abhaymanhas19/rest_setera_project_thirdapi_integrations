import React from 'react';
import { Outlet } from 'react-router-dom';

type Props = {};

const AccountsPageLayout = (props: Props) => {
  return (
    <><Outlet /></>
  );
};

export default AccountsPageLayout;